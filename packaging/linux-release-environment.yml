name: chatlog-release-linux
channels:
  - defaults
dependencies:
  # Exact linux-64 builds used by both release CI and the tag-bound build.
  - python=3.11.15=h741d88c_0
  - sqlite=3.53.2=h795bf6d_0
  - pip=26.1.2=pyhc872135_0
